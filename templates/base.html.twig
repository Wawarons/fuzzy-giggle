{% extends 'includes/header.html.twig' %}
{% block body %}
    {% if current_user() %}
        {# if user connected #}
    {% else %}
        {# if user is not connected #}
    {% endif %}
{% endblock %}